<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes WebPage</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white flex flex-col items-center py-4">
        <h1 class="py-6 text-3xl font-bold text-center uppercase ">Notes app</h1>
        <div class="form flex flex-col w-[80%] mb-10">
            <form action="/create" method="post">
                <input type="text" name="title" class="focus:outline-none w-full py-4 px-6 my-4 rounded-md text-black bg-zinc-100"
                    placeholder="Enter title here..">
                <textarea name="description" id="description" cols="30" placeholder="Enter your description" rows="5"
                    class="w-full rounded-md py-4 px-6 mb-6 text-black bg-zinc-100 focus:outline-none "></textarea>
                <input class="border w-48 py-3 rounded-lg hover:bg-zinc-500 font-bold uppercase cursor-pointer" type="submit"
                    value="Save"></input>
            </form>
        </div>
        <h5 class="w-[80%] mb-8 font-semibold text-2xl capitalize text-center tracking-tight">Get your created notes
            below</h5>

        <div class="flex flex-wrap w-[80%] items-center gap-6 ">

            <% if ( files.length> 0 ) { %>
                <% files.forEach( function(val){ %>
                    <div class="flex flex-col gap-2 rounded-md  w-[32%] p-4 text-black bg-zinc-300">
                        <div class="flex justify-between items-center">
                            <h6 class="text-xl capitalize"> <%= val.split('.')[0].split("_").join(" ") %> </h6>
                            <a href="/edit/<%= val %>"><i class="ri-pencil-line text-xl"></i></a>
                        </div>
                        <a class="text-md text-justify tracking-tighter text-blue-800 cursor-pointer"
                            href="/file/<%= val %>"> Read More.. </a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h4 class="text-center w-full">No Notes there..</h4>
                            <% } %>



        </div>



    </div>


</body>

</html>